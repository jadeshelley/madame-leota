# Core dependencies for Raspberry Pi - tested and working
groq==0.9.0
edge-tts==6.1.7
speechrecognition==3.10.0
pyttsx3==2.90
pygame==2.5.2
pyaudio==0.2.11

# System utilities
python-dotenv==1.0.0
requests==2.31.0
pillow==10.0.1

# Audio processing (simplified for Pi)
soundfile==0.13.1

# Note: Install these via system packages instead of pip:
# sudo apt install python3-opencv python3-numpy python3-pyaudio
# Then create virtual environment with: python3 -m venv --system-site-packages venv 