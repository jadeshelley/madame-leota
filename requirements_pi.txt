# Core dependencies for Raspberry Pi
openai==1.3.5
speechrecognition==3.10.0
pyttsx3==2.90
pygame==2.5.2
pyaudio==0.2.11

# System utilities
python-dotenv==1.0.0
requests==2.31.0
pillow==10.0.1

# Note: Install these via apt instead of pip:
# sudo apt install python3-opencv python3-numpy 